using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CompteSlime : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        UISpawnEnemy.compteur++;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
